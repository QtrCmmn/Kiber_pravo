# AGENT.md — КиберЩит / Kiber_pravo

Файл с правилами и контекстом для AI-агента (OpenCode).
**Читай этот файл перед любыми правками в проекте.**

---

## О проекте

- **Название:** КиберЩит
- **Репозиторий:** https://github.com/QtrCmmn/Kiber_pravo
- **Хостинг:** GitHub Pages
- **Стек:** Ванильный HTML + CSS + JavaScript (без фреймворков)
- **Назначение:** Веб-тренажёр по кибербезопасности — интерактивные сценарии-диалоги, где пользователь учится распознавать мошенников
- **Аудитория:** Школьники 10–17 лет. Язык — живой, разговорный, без канцелярита и сложных юридических терминов

---

## Структура файлов

```
/
├── index.html               # Главная страница
├── AGENT.md                 # Этот файл — правила для агента
├── .claude/
│   └── add-scenario.md      # Скилл: добавление нового сценария
├── js/
│   └── code.js              # Вся логика: тема, меню, сценарии, тренажёр
├── css/
│   └── style.css            # Стили, включая анимации (замена animate.css)
└── assets/
    ├── audio/               # Звуковые эффекты (mp3, preload="none")
    └── img/                 # Изображения плакатов (loading="lazy")
```

---

## Архитектурные решения

### Анимации
- `wow.js` и `animate.css` **удалены** — заменены нативным `IntersectionObserver`
- Классы `.wow` на элементах HTML сохранены для совместимости, не удалять
- Анимации управляются через CSS-классы: `anim-visible`, `anim-fade-up`, `anim-fade-down`, `anim-fade-left`, `anim-zoom-in`
- `data-wow-delay` продолжает работать (читается в JS как `el.dataset.wowDelay`)

### Тема (светлая / тёмная)
- Хранится в `localStorage` по ключу `'theme'`
- Переключается через атрибут `data-theme` на `<html>`
- Чекбокс `#theme-checkbox` управляет темой

### Бургер-меню
- Элементы: `#burger`, `.header__list`, `#menuOverlay`
- При открытии блокируется скролл (`document.body.style.overflow = 'hidden'`)
- Закрывается при клике на оверлей или пункт меню

### Тренажёр — сценарии
- Данные хранятся в массиве `scenarioBlueprints` в `code.js`
- Каждый сценарий содержит шаги (`steps`) с вариантами ответов
- Поля сценария: `id`, `title`, `tagline`, `difficulty`, `icon`, `attacker`, `intro`, `timeLimit`, `start`, `steps`

#### Структура шага (объектный формат):
```js
{
  messages: [
    { text: 'Текст от мошенника', delay: 600 }
  ],
  question: 'Как ответите?',
  options: [
    {
      text: 'Текст варианта',
      safe: true,           // true = правильный ответ
      feedback: 'Объяснение почему этот выбор верный/неверный',
      next: 'step2'         // id следующего шага, 'win' или 'lose'
    }
  ],
  onTimeout: {
    feedback: 'Что произошло при промедлении',
    next: 'lose'
  }
}
```

---

## Правила для агента

### Что можно
- Рефакторить `code.js` без изменения внешнего поведения
- Добавлять новые сценарии в массив `scenarioBlueprints` — см. `.claude/add-scenario.md`
- Улучшать CSS-анимации в `style.css`
- Оптимизировать производительность (lazy load, defer, async)

### Что нельзя
- Подключать внешние фреймворки (React, Vue, Angular и т.д.)
- Подключать новые CDN-зависимости без явного запроса автора
- Менять поля `id`, `title`, `difficulty` существующих сценариев — они могут использоваться в аналитике
- Удалять классы `.wow` с элементов HTML без замены логики анимаций
- Использовать `var` и jQuery
- Упрощать или формализовать язык текстов — аудитория это школьники, стиль живой

### Стиль кода
- ES6+: `const`/`let`, стрелочные функции, шаблонные строки
- Комментарии на **русском языке**
- `setTimeout` вместо `setInterval` для одноразовых задержек
- `DOMContentLoaded` вместо `window.onload` где возможно

### Стиль текстов сценариев
- Аудитория — школьники 10–17 лет: короткие фразы, никакого канцелярита
- Мошенник говорит убедительно, с давлением, иногда агрессивно
- Правильный ответ в `feedback` объясняет **почему** это безопасно, а не просто хвалит
- Неправильный ответ в `feedback` объясняет **чем именно** опасен этот выбор
- Длина одного сообщения мошенника — 1–2 предложения максимум
- Количество шагов в сценарии — от 6 до 10

---

## Процесс добавления сценария

При добавлении нового сценария **всегда читай** `.claude/add-scenario.md`.
Там полный чеклист, шаблон и примеры.

Краткий порядок:
1. Придумать реальную схему мошенничества (или взять из новостей)
2. Заполнить шаблон сценария по инструкции в `.claude/add-scenario.md`
3. Добавить объект в конец массива `scenarioBlueprints` в `code.js`
4. Проверить: каждый путь ведёт к `'win'` или `'lose'`, нет оборванных `next`

---

## Известные проблемы (backlog)

| # | Проблема | Приоритет |
|---|----------|-----------|
| 1 | `window.onload` + `setInterval` в прелоадере — заменить на `DOMContentLoaded` + `setTimeout` | Высокий |
| 2 | Шаги сценариев хранятся как плоский массив строк — перевести на объектный формат | Высокий |
| 3 | Тема читается из `localStorage` до гарантии наличия DOM — безопасно только пока скрипт в конце `<body>` | Средний |
| 4 | Аудиофайлы не проверены на сжатие (целевой битрейт: mp3 128 kbps) | Низкий |

---

## Сценарии (текущие)

| id | Название | Сложность |
|----|----------|-----------|
| `bank-call` | Звонок из банка | medium |
| `kufar-buyers` | Фейковые покупатели на Kufar | medium |
| `friend-link` | Сообщение от друга | easy |
| `relative-accident` | Родственник попал в ДТП | hard |
| `meter-investigator` | Счётчик, домофон и «следователь» | hard |
